<%- include('layout/header') %>
    <div class="conteiner">
        <h2>Relatório de acionamento dos dispositivos</h2>
        <div class="row my-4">
            <div class="col-lg-12">
                <div class="table-responsive">
                    <table class="table table-striped text-center">
                        <thead>
                            <tr class="table-dark">
                                <th>Nome</th>
                                <th>Dispositivo</th>
                                <th>Latitude</th>
                                <th>Longitude</th>
                                <th>Data</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% log.forEach((row, index)=> { %>
                                <tr class="align-middle">
                                    <td>
                                        <%= row.nome %>
                                    </td>
                                    <td>
                                        <%= row.dispositivo %>
                                    </td>
                                    <td>
                                        <%= row.latitude %>
                                    </td>
                                    <td>
                                        <%= row.longitude %>
                                    </td>
                                    <td>
                                        <%= row.data %>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <%- include('layout/footer') %>